# Cursor's Windows terminal cannot spawn cmd.exe (ENOENT / exit -4058).
# pnpm runs package scripts through this shell; PowerShell uses .ps1 shims and node.
script-shell=C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe
